defaults:
  - policy: ppo
  - reward: task1
  - training: default
  - wandb: default
  - experiment: ???
  - _self_

seed: 0
output_dir: outputs/${name}/${now:%Y-%m-%d}/${now:%H-%M-%S}

hydra:
  run:
    dir: ${output_dir}
  job:
    chdir: false
