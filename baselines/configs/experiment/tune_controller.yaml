# @package _global_
# Optuna-based controller parameter tuning (Appendix E.1)
name: tune_controller

building_type: ???
climate_zone: ???

n_trials: 200
n_startup_trials: 20
timeout_seconds: null
output_dir: configs/tuned_controllers
