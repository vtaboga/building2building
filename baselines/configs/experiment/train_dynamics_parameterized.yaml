# @package _global_
# Multi-building parameterized (building-param augmented) (Section 6.1)
name: dynamics_parameterized

defaults:
  - override /policy: ppo_parameterized
  - override /reward: task_const_e0
  - override /training: default

difficulty: easy
approach: parameterized

training:
  total_timesteps: 4_000_000
  n_envs: 16

env:
  pad_obs_to: 20
  normalize_obs: true
  augment_params: true
