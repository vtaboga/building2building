# Main configuration file for Building2Building project
defaults:
  - experiment: simulation
  - fetch_building: default
  - training: default
  - simulation: default
  - env: default
  - wandb: default
  - _self_

# Default experiment name will be set by experiment configs
# name: simulation  # Removed - should come from experiment configs

# Basic configuration
seed: 42
track: false

# Directories  
base_dir: results

# Hydra settings
hydra:
  run:
    dir: ${base_dir}/${name}_${now:%Y-%m-%d}_${now:%H-%M-%S}
  job:
    chdir: false 