defaults:
  - _self_
  - bldg: single_family
  - policy: on_off
  - reward: deadband

hydra:
  run:
    dir: outputs/${policy.type}/${now:%d-%m-%Y}/${now:%H-%M-%S}

env:
  normalize_obs: false
  max_steps: 35040

n_episodes: 1
