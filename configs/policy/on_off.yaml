type: on_off
target_temp_c: 21.0
deadband_c: 0.5
# Sensible-load request magnitude as a fraction of the inferred actuator bounds.
# Range: [-1, 1] where + is heating request and - is cooling request.
q_heat_pct: 0.8
q_cool_pct: 0.8

# Back-compat (deprecated): absolute watts. If provided, will be converted using inferred bounds.
# q_heat_w: 1000.0
# q_cool_w: 1000.0


