
defaults:
  - _self_
  - wandb: default
  - training: default
  - policy: ppo
  - reward: deadband
  - task: default
  - bldg: single_family
  - rollout: default

env:
  normalize_obs: true
  normalize_action: false
  max_steps: null

n_episodes: 1

seed: 42

# Benchmark-specific settings (used by scripts/bm_adaptive_dynamics.py and other problems).
benchmark:
  split: train
  start: 0
  limit: 0
  max_steps: null



